/*
Problem:
Check if an array contains any duplicate elements.

How I thought about it:
If inserting into a set fails, a duplicate exists.

Approach:
Use a hash set to track seen values.

Time Complexity: O(n)
Space Complexity: O(n)
*/
