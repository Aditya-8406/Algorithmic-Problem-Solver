/*
Problem:
Find the element that appears only once.

How I thought about it:
All other elements appear twice, so frequency tracking works.

Approach:
Use a hash map or XOR trick.

Time Complexity: O(n)
Space Complexity: O(1)
*/
